spack:
  packages:
    all:
      compiler: [gcc@11.4.0]
  specs:
  - cmake
  - pfunit~mpi~openmp
  - py-torch~cuda~mpi~openmp
  - py-torchvision
  - py-pip
  concretizer:
    unify: true
  config:
    install_missing_compilers: true
    install_tree: /opt/software
  view: /opt/views/view
