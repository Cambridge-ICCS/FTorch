spack:
  packages:
    all:
      compiler: [gcc@11.4.0]
  specs:
  - cmake
  - pfunit~mpi~openmp
  - py-torch~cuda~mpi~openmp
  - py-pip
  concretizer:
    unify: true
  config:
    install_missing_compilers: true
